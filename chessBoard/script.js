
let color = 1;
let boxSize = 40;
let ydist


function setup() {
  createCanvas(windowWidth, windowHeight)
  background(0);
  stroke(255);
}

function draw() {
  for(let i = 40;i>windowWidth; i += 40){
    for(let j = 40;j>windowheight;)
  }

}
